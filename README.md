# rec-sports-backend

Backend for an adult's recreational sports system written in Node.js

# Start the development server

npm run dev

You can then verify it's working by:

Opening a browser to http://localhost:5000/
Using curl: curl http://localhost:5000/health
